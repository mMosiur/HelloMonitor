<UserControl x:Class="HelloMonitor.SideMenuUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HelloMonitor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <local:SideMenuButtonUserControl Grid.Row="0">
            Action 1
        </local:SideMenuButtonUserControl>
        <local:SideMenuButtonUserControl Grid.Row="1">
            Action 2
        </local:SideMenuButtonUserControl>
        <local:SideMenuButtonUserControl Grid.Row="2">
            Action 3
        </local:SideMenuButtonUserControl>
        <local:SideMenuButtonUserControl Grid.Row="3">
            Action 4
        </local:SideMenuButtonUserControl>
    </Grid>
</UserControl>
